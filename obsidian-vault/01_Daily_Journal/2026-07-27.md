# Daily Journal - 2026-07-27

## Accomplishments
- **Storefront (Phase 1.5) Completed:** We built the fully functional Next.js storefront for customers. 
  - Subdomain routing works (`ali.shopo.pk` -> `slug: ali`).
  - Read-only Public API (`/v1/public/...`) handles fetching stores, products, and placing orders.
  - Cart state and checkout flow successfully integrated.
- **Merchant Mobile App Kickoff (Phase 2):** Starting the React Native (Expo) merchant app.
  - Adding Auth (`/v1/auth/*`), Stores (`/v1/stores`), and Dashboard stats endpoints to the backend.
  - Moving to JWT-based authentication using `expo-secure-store`.
  - Adding order status mutations (`PATCH /v1/orders/:id/status`).

## Architecture Decisions
- Backend uses `bcryptjs` for local password hashing and generates JWTs using `SUPABASE_JWT_SECRET`.
- The Mobile app uses a global `apiFetch` helper that reads the token from `expo-secure-store`.

## Next Steps
1. Build out the backend APIs.
2. Install Expo dependencies.
3. Build the Auth screens and tab layout.
4. Implement Dashboard, Products, Orders, and Settings screens.
