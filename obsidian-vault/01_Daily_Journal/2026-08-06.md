# Daily Journal — 2026-08-06

## Accomplishments
- **Expo Router Navigation Re-Architecture (Mobile App):**
  - Re-architected mobile app layout tree from `<Tabs>` containing `(stack)` to a `<Stack>` Navigator wrapping `<Tabs>` (`(app)/(tabs)`).
  - Moved Home (`index`), Products (`products`), Orders (`orders`), and Settings (`more`) into `(app)/(tabs)`.
  - Placed all sub-screens (`notifications`, `notifications-inbox`, `language`, `store/create`, `analytics`, `customers`) on the root `(app)` Stack Navigator.
  - Restored **100% dynamic natural stack back navigation** across the app without hardcoded redirects.

- **Mobile UI Alignment & Safe Inset Polish:**
  - Restored spacious horizontal scrolling stats cards (`Total Sales`, `Orders`, `Products`) on Home Dashboard with end scroll padding (`paddingRight: 28`).
  - Added safe bottom insets (`paddingBottom: Math.max(insets.bottom, 24)`) to `Save Preferences` and `Create My Shop` bottom action bars to clear the Android software navigation bar (`||| O <`).
  - Added top status bar notch clearance (`paddingTop: Math.max(insets.top, StatusBar.currentHeight || 24, 16)`).
  - Purged mock arrays (`Sarah Khan`, mock notifications) from `notifications-inbox.tsx` and connected screen to backend API `/merchant/notifications`.

- **Auth Loop Resolution:**
  - Fixed infinite 3-screen auth loop by updating root `src/app/index.tsx` from `<Redirect href="/(auth)/welcome" />` to `<Redirect href="/(app)" />`.
  - Updated `AuthGuard` in `src/app/_layout.tsx` to re-evaluate token state on route segment transitions so logging in or registering immediately enters the app.

- **Expo Router Typed Routes Clean-Up:**
  - Fixed Expo Router typed routes warnings across 10 files (`login.tsx`, `confirmation.tsx`, `category.tsx`, `products/index.tsx`, `upload.tsx`, `analytics.tsx`, `customers/index.tsx`, `language.tsx`, `launch.tsx`, `_layout.tsx`).

## Architecture Decisions
- [[02_Architecture/ADR-014-stack-wrapping-tabs|ADR-014]]: Standardized Expo Router layout to Stack-wrapping-Tabs so stack screens render over tabs and pop back dynamically to their exact originating screen.
- [[02_Architecture/ADR-015-dynamic-auth-guard|ADR-015]]: Updated root entry route `index.tsx` to redirect to `/(app)` and configured `AuthGuard` to verify token on route segment changes.

## Anti-Patterns Recorded
- [[05_Anti_Patterns/hidden-stack-in-tabs-navigator]]: Registering stack screens inside a hidden `<Tabs>` tab breaks stack history and causes `router.back()` to jump to `firstRoute` (Home tab).
- [[05_Anti_Patterns/hardcoded-root-redirect-loop]]: Placing `<Redirect href="/(auth)/welcome" />` in root `index.tsx` causes infinite redirect loops after login.

## AI Model Used
- Antigravity AI Engine (Google Gemini 2.5 Flash / Claude hybrid)

## Tags
#daily #navigation #expo-router #ui-polish #auth-fix
