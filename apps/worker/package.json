{
  "name": "@shopo/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "cd ../../packages/database && pnpm run build && cd ../../packages/shared && pnpm run build",
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@shopo/database": "workspace:*",
    "@shopo/shared": "workspace:*",
    "@google/genai": "^2.13.0",
    "bullmq": "^5.7.8",
    "dotenv": "^16.4.5",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.7.1",
    "typescript": "^5.7.0"
  }
}
